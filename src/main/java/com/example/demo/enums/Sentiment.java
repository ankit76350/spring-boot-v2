package com.example.demo.enums;

// FIXED: Moved from 'com.example.demo.enum' to 'com.example.demo.enums'
// because 'enum' is a reserved keyword in Java and cannot be a package name.
public enum Sentiment {
    HAPPY,
    SAD,
    ANGRY,
    ANXIOUS
}
